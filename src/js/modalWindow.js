define(['jquery', 'bootstrap'], function($) {
    var modalWindow;
    return modalWindow = (function() {
        function modalWindow() {
            $('#myModal').modal('show');
            return true;

        }

        return modalWindow;
    })();
});